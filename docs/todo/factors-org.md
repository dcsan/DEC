factors

When creating axes for the factors rather than just one statement, I want to know what the range is from -> to

For example, we came up with just this one factor called:
Organizational structure and bureaucracy

but its not clear what the slider value would be left or right

instead come up with labels for each end.
so redo the schema for the request to be:

label: organizational structure
left: bureaucratic
right: freeform

eg the two left/right labels should be opposite values on the same scale.


