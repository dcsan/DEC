modify the 2x2 matrix widget

the user would need to provide a minimum of two choices
the LLM then needs to extract two axes to compare those items

eg "should i buy a car or a motorbike"

two axes:
cost: cheap->expensive X axis
safety: risky vs safe

* interaction:
make it more a freeform box the user can drag items onto
when we then provide the results we'd score each item with a value for either property
from 0 to 100 on these axes
eg 'safety' from 0 = not safe to 100 = very safe

the user can drag and drop / position an item anywhere onto that grid, not just in the four corners
when the form is submitted we score each item with those values

