add some more features to the '2x2' matrix choice.

* more axes
at the bottom show a button to 'redo axes'
this would use an LLM to choose some extra options for the two axes
to better compare the featured items
it would then present a list of axes
and let the user choose up to two options and 'set'
on setting those new axes, we would also submit all the current items on the grid
the LLM would then re-score those items on the new axes
and position on response.

* more options
the widget would suggest other similar options and also score them
eg car, bike -> other types of personal transport
and pre-place them on the grid.

