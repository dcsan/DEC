convo router

create a lightweight router

this should classify user inputs

is it a decision?
    if so
        search widget 'purpose' fields
        then show the most relevant widget

    if not:
        interpret the input based on conversation history

