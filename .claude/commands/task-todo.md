this script takes the name of a task in the 'docs/todo' folder

find the named item

implement the feature

add a short oneline description to the doc with what was done like:

---
done: <note>
at: <timestamp>

move the file from docs/todo/ to docs/done

